export interface Record {
    action: string,
    completed: boolean,
    date: string,
    ticker: string,
    strikeDate: string,
    strikePrice: number,
    contracts: number,
    price: number,
}